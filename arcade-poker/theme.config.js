/** @type {const} */
const themeColors = {
  primary: { light: '#00FF41', dark: '#00FF41' },       // ネオングリーン
  background: { light: '#0D1B0F', dark: '#0D1B0F' },    // ダークグリーン背景
  surface: { light: '#1A2E1C', dark: '#1A2E1C' },       // カード台（深緑）
  foreground: { light: '#E8F5E9', dark: '#E8F5E9' },    // メインテキスト
  muted: { light: '#4CAF50', dark: '#4CAF50' },         // サブテキスト
  border: { light: '#2E7D32', dark: '#2E7D32' },        // ボーダー
  success: { light: '#00FF41', dark: '#00FF41' },       // 成功（ネオングリーン）
  warning: { light: '#FFD700', dark: '#FFD700' },       // 警告（ネオンゴールド）
  error: { light: '#FF3131', dark: '#FF3131' },         // エラー（ネオンレッド）
  tint: { light: '#00FF41', dark: '#00FF41' },          // タブアクティブ色
};

module.exports = { themeColors };
