{
  "installCommand": "corepack enable && cd arcade-poker && corepack pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && cd arcade-poker && corepack pnpm run build:vercel",
  "outputDirectory": "arcade-poker/dist",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
